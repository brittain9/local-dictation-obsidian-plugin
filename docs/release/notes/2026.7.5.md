## Fixes

- **Restored the Community Plugins listing.** Removed the redundant "for Obsidian" phrasing from the plugin description so it passes the community-store validator, which rejects descriptions that repeat "Obsidian". No functional changes — a metadata-only release to get the plugin back online.
